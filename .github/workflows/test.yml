name: test backend
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          set -x
          docker version
          docker compose up -d backend postgres
          cp dev/.env .env
          ./run-cmd.sh pytest --cov-report term
